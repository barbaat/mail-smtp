{
  "name": "smtp-mailer",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación local para enviar correos SMTP individuales a múltiples destinatarios.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check api/index.js && node --check routes/mail.js && node --check services/smtpMailer.js && node --check utils/recipients.js && node --check utils/validation.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "multer": "^2.0.2",
    "nodemailer": "^6.9.16",
    "sanitize-html": "^2.13.1",
    "validator": "^13.12.0"
  },
  "devDependencies": {}
}
